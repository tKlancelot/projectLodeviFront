import { AdvertPricePipe } from './advert-price.pipe';

describe('AdvertPricePipe', () => {
  it('create an instance', () => {
    const pipe = new AdvertPricePipe();
    expect(pipe).toBeTruthy();
  });
});
